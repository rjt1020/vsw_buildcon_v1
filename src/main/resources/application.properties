# =============================================================
# [MODIFIED] Database Configuration Removed for Now
# =============================================================

# =============================================================
# Email Configuration (GoDaddy/Reusing 2b1.co.in style)
# =============================================================
spring.mail.host=smtpout.secureserver.net
spring.mail.port=465
spring.mail.username=sales@2b1.co.in
spring.mail.password=${GODADDY_EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory

# =============================================================
# Application Settings
# =============================================================
server.port=8080
# Serve static resources directly from resources/static (Strict frontend protocol)
spring.web.resources.static-locations=classpath:/static/